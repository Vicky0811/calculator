apiVersion: v1
kind: Service
metadata:
  name: java-calculator-svc
spec:
  type: NodePort
  selector:
    app: java-calculator
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080